# 限价止盈委托实现总结

## 实现方式对比

### 用户提供的交易所格式

```
2026-03-13 08:48:27 BTCUSDT 永续
类型：限价委托
卖出
71,586.00
数量：0.060 BTC
已成交数量：0.000 BTC
是否减仓：是
只做 MARK：否
触发条件：–
```

### 我们的代码实现

```python
result = self.order_executor.client.client.new_order(
    symbol=symbol,
    side=take_profit_side,  # 与持仓方向相反
    type="LIMIT",  # ✅ 限价委托
    price=rounded_price,  # ✅ 止盈价格
    quantity=rounded_size,  # ✅ 平仓数量
    timeInForce="GTC",  # ✅ 一直有效直到成交或取消
    reduceOnly=True  # ✅ 只减仓，不平仓开新仓
)
```

## 参数对照表

| 交易所字段 | 值 | 代码参数 | 说明 |
|-----------|-----|---------|------|
| **类型** | 限价委托 | `type="LIMIT"` | 限价订单 |
| **方向** | 卖出 | `side=take_profit_side` | 与持仓方向相反（LONG→SELL, SHORT→BUY） |
| **价格** | 71,586.00 | `price=rounded_price` | 止盈价格（四舍五入到交易所精度） |
| **数量** | 0.060 BTC | `quantity=rounded_size` | 平仓数量（四舍五入到交易所精度） |
| **是否减仓** | 是 | `reduceOnly=True` | 只减仓，不平仓开新仓 |
| **有效期** | 一直有效 | `timeInForce="GTC"` | Good Till Cancelled |
| **触发条件** | – | 无 | 直接挂单，无需触发条件 |

## 核心优势

### 1. 简单直接
- ✅ 不需要条件单 API
- ✅ 不需要 stopPrice 触发价格
- ✅ 委托直接挂在订单簿上
- ✅ 价格到达后立即成交

### 2. 减模式
```python
reduceOnly=True
```
- 确保委托只能减少现有持仓
- 不能在平仓时开新仓
- 符合交易所"是否减仓：是"的要求

### 3. 有效期管理
```python
timeInForce="GTC"
```
- Good Till Cancelled
- 委托一直有效直到成交或被取消
- 不需要重复提交

### 4. 精度处理
```python
rounded_price = self.order_executor._round_price(symbol, take_profit_price)
rounded_size = self.order_executor._round_quantity(symbol, position_size)
```
- 自动四舍五入到交易所精度
- 避免因精度问题导致的委托失败

## 工作流程

### 多头持仓止盈
```
持仓：LONG 0.064 BTC @ 70000
止盈：SELL 0.064 BTC @ 71586.00, reduceOnly=True

委托提交后：
- 挂在卖单簿上
- 当市场价格 >= 71586.00 时成交
- 成交后持仓减少 0.064 BTC
- 不会开新仓
```

### 空头持仓止盈
```
持仓：SHORT 0.064 BTC @ 70000
止盈：BUY 0.064 BTC @ 68500.00, reduceOnly=True

委托提交后：
- 挂在买单簿上
- 当市场价格 <= 68500.00 时成交
- 成交后持仓减少 0.064 BTC
- 不会开新仓
```

## 暴露量处理

### 问题
之前使用 `order_executor.open_position()` 会触发暴露量检查，导致止盈委托失败：
```
暴露量超限：持仓=0.0640, 现有委托=0.0000, 新委托=0.0640, 总计=0.1280
```

### 解决方案
直接调用 API，跳过 `open_position()` 的暴露量检查：
```python
# 直接调用 API，跳过 order_executor.open_position() 的暴露量检查
# 因为 reduceOnly=True 的委托不会增加暴露量
result = self.order_executor.client.client.new_order(
    symbol=symbol,
    side=take_profit_side,
    type="LIMIT",
    price=rounded_price,
    quantity=rounded_size,
    timeInForce="GTC",
    reduceOnly=True
)
```

### 为什么可以跳过暴露量检查？
- **暴露量定义**：持仓 + 未成交委托（开仓方向）
- **reduceOnly=True**：委托只能减仓，不能开仓
- **结论**：reduceOnly 委托不会增加暴露量，所以不需要检查

## 日志输出

### 成功提交
```
[ORDER] 创建止盈委托：SELL 0.0640 @ 71842.06 (止盈/止损) - 持仓方向：LONG
[ORDER] ✅ 限价止盈委托已提交：订单 ID=12345678
[ORDER] 止盈详情：方向=SELL, 价格=71842.06, 数量=0.0640, reduceOnly=True
[ORDER] 委托类型：限价止盈（挂单等待成交）
```

### 委托详情
```
订单 ID: 12345678
类型：LIMIT (限价委托)
方向：SELL (卖出)
价格：71842.06
数量：0.0640 BTC
reduceOnly: True
时间：GTC (一直有效)
状态：NEW (已提交，等待成交)
```

## 与条件单的对比

| 特性 | 限价止盈委托 | 条件单 (STOP_MARKET) |
|------|------------|-------------------|
| **订单类型** | LIMIT | STOP_MARKET |
| **触发条件** | 无条件，直接挂单 | 需要 stopPrice 触发 |
| **成交方式** | 限价成交 | 触发后市价成交 |
| **价格确定性** | ✅ 价格确定 | ❌ 成交价不确定 |
| **成交保证** | ❌ 可能不成交 | ✅ 触发后保证成交 |
| **订单簿显示** | ✅ 显示在订单簿 | ❌ 不显示 |
| **实现复杂度** | 简单 | 较复杂 |
| **API 要求** | 普通订单接口 | 普通订单接口 |

## 验证结果

所有 10 项验证全部通过：

✅ 使用 LIMIT 限价委托
✅ 设置 reduceOnly=True（只减仓）
✅ 设置 GTC（一直有效）
✅ 无条件（stopPrice 未设置）
✅ 四舍五入到交易所精度
✅ 方向与持仓相反
✅ 条件单相关代码已移除
✅ 注释说明跳过暴露量检查
✅ 包含完整的日志输出
✅ Python 语法正确

## 修改文件

- `f:\Quant-Agent\ai_quant_trader\core\scheduler.py` (第 1357-1401 行)
- 修复脚本：`f:\Quant-Agent\fix_limit_take_profit.py`
- 验证脚本：`f:\Quant-Agent\verify_limit_take_profit.py`

## 总结

通过使用**普通限价委托 + reduceOnly=True**，我们实现了与交易所完全一致的止盈委托方式：

1. ✅ 简单直接，不需要条件单
2. ✅ reduceOnly 确保只减仓
3. ✅ GTC 有效期管理
4. ✅ 价格和数量自动四舍五入
5. ✅ 跳过暴露量检查（reduceOnly 委托不增加暴露量）
6. ✅ 完整的日志输出
7. ✅ 完全符合交易所格式

现在系统可以正确创建限价止盈委托，挂单等待价格到达后自动成交，锁定利润！
