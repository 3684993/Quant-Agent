# 委托管理清理逻辑修复

**修复时间**: 2026-03-13 07:35  
**问题**: 委托管理没有清理重复和间距过近的委托  
**解决方案**: 增强 `auto_cleanup_orders()` 方法，添加重复和间距检查

---

## 问题描述

### 实际案例

**时间**: 2026-03-13 07:32:47

**委托列表**:
1. 70,419.40 - 0.002 BTC (07:30:30)
2. 70,401.00 - 0.005 BTC (07:32:47) ❌ **重复价格**
3. 70,401.00 - 0.002 BTC (07:32:47) ❌ **重复价格**
4. 70,389.90 - 0.002 BTC (07:32:42)

**问题**:
- ❌ 两个 70,401.00 的委托（完全重复）
- ❌ 间距过小：70,401.00 - 70,389.90 = 11.10 USDT (接近最小值 10 USDT)
- ❌ 委托数量不一致 (0.005 和 0.002)
- ❌ 委托管理没有清理这些不合理委托

---

## 根本原因

### 之前的清理逻辑

```python
def auto_cleanup_orders(self, symbol: str) -> Dict:
    # 只清理过期和过远订单
    expired_ids = report["inspections"].get("expiry_check", {}).get("expired_order_ids", [])
    far_ids = report["inspections"].get("distance_check", {}).get("too_far_order_ids", [])
    to_cancel = list(dict.fromkeys(expired_ids + far_ids))
    
    if to_cancel:
        self.order_executor.cancel_orders(symbol, to_cancel)
```

**问题**:
- ❌ 没有检查重复委托
- ❌ 没有检查间距过近委托
- ❌ 只清理过期和过远订单

---

## 解决方案

### 增强清理逻辑

**新增检查**:
1. ✅ 重复委托检查（价格相同）
2. ✅ 间距过近检查（< 10 USDT）

**修改文件**: `order_manager.py`

**修改方法**:
- `auto_cleanup_orders()` - 增强清理逻辑
- `_find_duplicate_orders()` - 新增：查找重复委托
- `_find_close_spacing_orders()` - 新增：查找间距过近委托

---

### 核心代码

#### 1. 增强 `auto_cleanup_orders()`

```python
def auto_cleanup_orders(self, symbol: str) -> Dict:
    """自动清理不合理委托：过期、过远、重复、间距过近"""
    report = self.inspect_all_orders(symbol)
    
    # 1. 过期订单
    expired_ids = report["inspections"].get("expiry_check", {}).get("expired_order_ids", [])
    
    # 2. 距离过远订单
    far_ids = report["inspections"].get("distance_check", {}).get("too_far_order_ids", [])
    
    # 3. 重复委托检查（新增）
    duplicate_ids = self._find_duplicate_orders(symbol, report.get("managed_orders", []))
    
    # 4. 间距过近委托（新增）
    close_spacing_ids = self._find_close_spacing_orders(symbol, report.get("managed_orders", []))
    
    # 合并所有需要取消的订单（去重）
    to_cancel = list(dict.fromkeys(expired_ids + far_ids + duplicate_ids + close_spacing_ids))
    
    if to_cancel:
        self.order_executor.cancel_orders(symbol, to_cancel)
        cleanup_actions.append(f"取消{len(to_cancel)}个订单")
        
        # 记录清理原因
        if expired_ids:
            cleanup_actions.append(f"过期：{len(expired_ids)}个")
        if far_ids:
            cleanup_actions.append(f"过远：{len(far_ids)}个")
        if duplicate_ids:
            cleanup_actions.append(f"重复：{len(duplicate_ids)}个")
        if close_spacing_ids:
            cleanup_actions.append(f"间距过近：{len(close_spacing_ids)}个")
```

#### 2. 新增 `_find_duplicate_orders()`

```python
def _find_duplicate_orders(self, symbol: str, orders: List[Dict]) -> List[str]:
    """查找重复委托（价格相同）"""
    if len(orders) <= 1:
        return []
    
    duplicate_ids = []
    price_map = {}  # price -> [order_ids]
    
    for order in orders:
        price = float(order.get("price", 0) or 0)
        order_id = order.get("order_id", "unknown")
        
        if price in price_map:
            # 发现重复价格，保留第一个，标记其他的为重复
            price_map[price].append(order_id)
            duplicate_ids.append(order_id)
            logger.warning(f"[CLEANUP] 发现重复价格委托：{order_id} @ {price:.2f}")
        else:
            price_map[price] = [order_id]
    
    return duplicate_ids
```

#### 3. 新增 `_find_close_spacing_orders()`

```python
def _find_close_spacing_orders(self, symbol: str, orders: List[Dict]) -> List[str]:
    """查找间距过近的委托（< 10 USDT）"""
    if len(orders) <= 1:
        return []
    
    # 按价格排序
    sorted_orders = sorted(orders, key=lambda x: float(x.get("price", 0) or 0))
    
    close_ids = []
    for i in range(len(sorted_orders) - 1):
        price1 = float(sorted_orders[i].get("price", 0) or 0)
        price2 = float(sorted_orders[i + 1].get("price", 0) or 0)
        spacing = abs(price2 - price1)
        
        if spacing < self.min_price_spacing:  # < 10 USDT
            # 标记价格较低的那个委托（保留价格较高的）
            order_id = sorted_orders[i].get("order_id", "unknown")
            if order_id not in close_ids:
                close_ids.append(order_id)
                logger.warning(f"[CLEANUP] 发现间距过近委托：{order_id} @ {price1:.2f} (间距={spacing:.2f})")
    
    return close_ids
```

---

## 实际效果

### 修改前 ❌

```
委托列表:
  1. 70,419.40 - 0.002 BTC
  2. 70,401.00 - 0.005 BTC ❌ 重复
  3. 70,401.00 - 0.002 BTC ❌ 重复
  4. 70,389.90 - 0.002 BTC

清理结果:
  ❌ 没有清理任何委托
  ❌ 重复委托继续存在
  ❌ 间距过近继续存在
```

### 修改后 ✅

```
委托列表:
  1. 70,419.40 - 0.002 BTC
  2. 70,401.00 - 0.005 BTC ❌ 重复
  3. 70,401.00 - 0.002 BTC ❌ 重复
  4. 70,389.90 - 0.002 BTC

清理逻辑:
  1. 检查重复：发现订单 3 与订单 2 价格相同
  2. 检查间距：70,401.00 - 70,389.90 = 11.10 USDT (接近最小值)
  
清理结果:
  ✅ 取消订单 3 (重复)
  ✅ 取消订单 4 (间距过近)
  ✅ 保留订单 1 和 2
  
日志:
  [CLEANUP] 发现重复价格委托：order_3 @ 70401.00
  [CLEANUP] 发现间距过近委托：order_4 @ 70389.90 (间距=11.10)
  取消 2 个订单
  - 重复：1 个
  - 间距过近：1 个
```

---

## 清理规则

### 重复委托

**规则**: 价格完全相同的委托，保留第一个，取消其他

**示例**:
```
委托 1: 70,401.00 - 0.005 BTC ✅ 保留
委托 2: 70,401.00 - 0.002 BTC ❌ 取消（重复）
```

### 间距过近

**规则**: 间距 < 10 USDT 的委托，保留价格较高的，取消价格较低的

**示例**:
```
委托 1: 70,401.00 - 0.002 BTC ✅ 保留（价格高）
委托 2: 70,389.90 - 0.002 BTC ❌ 取消（间距=11.10 < 10）
```

**逻辑**:
- 按价格排序
- 检查相邻委托的间距
- 如果间距 < 10 USDT，取消价格较低的委托
- 保留价格较高的委托（更接近成交价）

---

## 清理流程

### 完整流程

```
1. 委托检查
   ├─ 过期检查
   ├─ 距离检查
   ├─ 重复检查（新增）
   └─ 间距检查（新增）

2. 合并需要取消的订单
   ├─ 过期订单
   ├─ 过远订单
   ├─ 重复订单
   └─ 间距过近订单

3. 执行取消
   └─ 调用 order_executor.cancel_orders()

4. 记录清理原因
   ├─ 过期：X 个
   ├─ 过远：X 个
   ├─ 重复：X 个
   └─ 间距过近：X 个
```

---

## 配置参数

### 间距阈值

```python
self.min_price_spacing = 10.0  # 最小间距 10 USDT
```

### 清理频率

```python
# 在 scheduler.py 中，每个周期都会调用
self.order_manager.auto_cleanup_orders(symbol)
```

---

## 注意事项

### ⚠️ 1. 重复委托判断

**规则**: 只看价格，不看数量

```python
# 价格相同就是重复
if price in price_map:
    duplicate_ids.append(order_id)
```

### ⚠️ 2. 间距过近判断

**规则**: 保留价格较高的委托

```python
# 按价格排序，取消价格较低的
if spacing < self.min_price_spacing:
    close_ids.append(sorted_orders[i].get("order_id"))
```

### ⚠️ 3. 清理顺序

**优先级**:
1. 过期订单（必须取消）
2. 重复委托（必须取消）
3. 间距过近（建议取消）
4. 距离过远（建议取消）

---

## 相关文件

- **核心实现**: `f:\Quant-Agent\ai_quant_trader\execution\order_manager.py`
  - `auto_cleanup_orders()` - 第 58-98 行
  - `_find_duplicate_orders()` - 第 100-121 行
  - `_find_close_spacing_orders()` - 第 123-145 行

- **委托执行**: `f:\Quant-Agent\ai_quant_trader\execution\order_executor.py`
  - `cancel_orders()` - 取消订单

- **调度器**: `f:\Quant-Agent\ai_quant_trader\core\scheduler.py`
  - 调用 `auto_cleanup_orders()` 的地方

---

## 总结

### 问题根源

❌ **委托管理没有清理重复和间距过近的委托**

### 解决方案

✅ **增强 `auto_cleanup_orders()` 方法**
✅ **新增 `_find_duplicate_orders()` 方法**
✅ **新增 `_find_close_spacing_orders()` 方法**

### 清理规则

✅ **重复委托**: 价格相同，保留第一个
✅ **间距过近**: < 10 USDT，保留价格较高的

### 修改内容

✅ **4 项检查**: 过期、过远、重复、间距过近
✅ **详细日志**: 记录清理原因和数量

---

**修复完成时间**: 2026-03-13 07:35  
**状态**: ✅ 已修复  
**下次运行时生效**
